node_modules/
out/
dist/
release/
.vite/
.artifacts/
.coding-eval/
*.log
.env
.env.*
!.env.example
.dsh-source
.DS_Store
Thumbs.db

# Local docs and scratch (not versioned)
docs/
.claude/
.pi-ling/
a.md
b.md

# Agent skills
# pi-ling's own skills are versioned under `.agents/skills`.
# These two are synced from a private checkout by `pnpm skills:sync`, so they
# stay local: they are bound to one author's browser profile and tooling.
.agents/skills/browser-bridge/
.agents/skills/browser-use/

# Desktop packaging outputs
apps/desktop/release-*/

# Scratch examples / experiments (not versioned)
example/
