/** Default model↔tool round trips per user run (Native agent loop). */
export const DEFAULT_MAX_TURNS = 50;

/** Retries for transient provider stream failures such as `terminated`. */
export const DEFAULT_STREAM_RETRIES = 3;

export const DEFAULT_STREAM_RETRY_BASE_MS = 1_000;
