# @pi-ling/native-runtime

`RuntimeAdapter` implementation over `@pi-ling/coding-agent`.

It maps Native Agent text, thinking, tools, permissions, cancellation, and
terminal state to the same runtime event vocabulary used by DSH.
