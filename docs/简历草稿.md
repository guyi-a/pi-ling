# 王智林

**求职方向：** AI Agent 工程 / AI 应用开发

**南开大学 · 软件工程 · 2023.09 — 2027.06**

2903988117@qq.com · 16629057092 · github.com/guyi-a

---

## 实习经历

**快手 · 可灵 · AI 应用开发实习生**  
2026.03 — 至今

- **MCP 接入**：搭建 Agent 侧 MCP 客户端，统一维护多 Server 的连接配置、凭证与生命周期；支持远程 Server 的 OAuth 授权接入。工具元数据缓存与有状态连接解耦——清单本地缓存、调用时建连、连接跨会话复用。
- **Subagent 委派**：内置只读探索 Subagent 承接检索类任务，搜索与读文件在隔离子上下文执行；仅暴露只读工具，禁止写入与再委托。上下文独立，主 Agent 只接收结构化摘要；委派方式分前台与后台两种。
- **长耗时任务后台化**：针对视频生成等数十分钟级调用阻塞整轮对话、应用重启后任务丢失的问题，设计持久化后台任务链路；任务意图先落库、登记远端 ID 后提交即返回。调度按任务类型限并发，完成后自动回调主 Agent；重启后依持久化表恢复续跑。

---

## 项目经历

### Pi-Ling · Coding Agent 产品工作台（个人项目）

**技术栈：** Electron · React · TypeScript · SQLite · pi-agent · DSH · Claude Agent SDK

**项目描述：** 基于 pi-agent 搭建的 Electron 桌面 Coding Agent 工作台，将 Native、DSH、Claude 等多套 Agent 引擎收敛到统一产品层；围绕引擎接入、跨引擎对齐、上下文治理与行为回归四条主线建设。

- **可插拔 Agent 引擎**：搭建统一 Runtime 适配层，接入 Native（pi-agent）、DSH、Claude Code 三套引擎；产品层统一管理会话生命周期与历史上下文，支持会话内切换引擎且审批语义保持一致。
- **DSH 桥接与跨内核对齐**：开发 sidecar 插件使 DSH 共享产品历史——历史同步（全量 seed + 增量追平）、审批对齐（manual / accept-write / auto 三档映射）、上下文压缩（与 Native 共用规则）。
- **工具审批与上下文治理**：将工具调用后果建模为 effect，按 manual / accept-write / auto 三档控制审批粒度；超大 tool 结果 spill 落盘按需读取；上下文逼近窗口上限时轮间滚动摘要压缩，保留近轮完整对话、仅折叠更早历史。
- **Agent 行为回归评测**：搭建 task catalog，每题在隔离 git worktree 还原 fixture；客观层以 verify 命令 + diff 判 pass/fail，主观层以 LLM-as-judge 按 rubric 打分；支持 baseline / candidate 配对对比通过率，Eval 面板承载评测入口。

---

## 技能

TypeScript · Go · Python · React · Electron · SQLite · Git · AI Agent 工程 · LLM 应用开发

---

## 制作备注（不进 PDF）

**下一环：** 内容确认后，按 `E:\lingcowork\data\skills\builtin\pdf\SKILL.md` 路径 B 制作：

1. HTML 初稿：`E:\pi-ling\docs\简历.html`（基于 `wzl-resume.html` 样式，浏览器打开可编辑）。
2. 确认后执行 `uv run E:\lingcowork\data\skills\builtin\pdf\scripts\html_to_pdf.py --input E:\pi-ling\docs\简历.html --output E:\pi-ling\docs\简历.pdf`。

**与旧版 HTML 的差异（待同步）：**

- 个人项目仅 **Pi-Ling**（不含 LingCoWork）；实习三条：MCP / Subagent 委派 / 后台任务。
- pi-ling 第三条已写入 effect 三档审批、tool spill、滚动摘要压缩；第四条补 LLM-as-judge。
